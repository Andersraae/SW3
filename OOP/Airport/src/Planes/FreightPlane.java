package Planes;

import java.util.Date;

public class FreightPlane extends Plane {
    public FreightPlane(int planeId, Date departureTime, Date arrivaltime, String state) {
        super(planeId, departureTime, arrivaltime, state);
    }
}
